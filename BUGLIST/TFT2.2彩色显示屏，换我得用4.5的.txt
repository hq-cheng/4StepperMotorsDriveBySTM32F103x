// 一定在一开始就确定好方案，不然你到最后还得换一套方案，就得重新测试一套

// 对于这些显示屏来说，一般是带有专用的控制芯片的，如ILI9340
// 可以并行，也可串行（学习串行吧），串行采用SPI接口（SPI通信协议）	

// SDO		CD的串行口输出

// LED		背光正极，其可以接IO管脚（背光可以由IO口提供电流），或者外接3.3V电压	

// SCK		LCD的串行口时钟

// SDI		LCD的串行口输入

// DC/RS	Data / Command	
		When DCX = ’1’, data is selected. 
		When DCX = ’0’, command is selected.

// RESET	接单片机的IO管脚(或接单片机的复位信号—实际试验  无效)

// CS		片选

// GND

// VCC		供电电压2.8~3.3V，背光也是2.8~3.3V，绝对不能接5V


// GUI库显示整个图片程序？
// GUI库显示数字，或字母程序？
	---->解决，不就是比较画点嘛，简单


// 为什么TFT一般的控制芯片会有设置/开辟一个区域的功能呢？
	1. 灵活方便啊，不用刷新整个屏幕，岂不是很好；
	2. 因为一般来说字体，图片本身数据量就占一部分，然后每次设置坐标，还得发送（好像是5次）命令/数据，这样数据量就增加了5倍，岂不是凉凉；而开辟这个区域只用设置窗口大小和扫描方向就可以了；
